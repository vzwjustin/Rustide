---
phase: 01-foundation-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ui_shell/Cargo.toml
autonomous: true

must_haves:
  truths:
    - "Application compiles without errors on macOS"
    - "Application launches and displays main window"
    - "User sees editor pane with empty state message"
  artifacts:
    - path: "target/release/ui_shell"
      provides: "Compiled binary for macOS"
  key_links:
    - from: "crates/ui_shell/src/main.rs"
      to: "RustideApp::run()"
      via: "main() entry point"
      pattern: "app\\.run\\(\\)"
---

<objective>
Validate that the Rustide application compiles and launches on macOS.

Purpose: This is the first gate - we need to confirm the existing scaffolding actually works before modifying it. This validates INFRA-01 and establishes baseline for all subsequent work.

Output: Working macOS application binary that launches, displays window, and renders the editor UI.
</objective>

<execution_context>
@/Users/justinadams/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinadams/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-validation/01-RESEARCH.md
@crates/ui_shell/src/main.rs
@crates/ui_shell/src/app.rs
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile release build for macOS</name>
  <files>target/release/ui_shell</files>
  <action>
Run `cargo build --release -p ui_shell` to compile the application.

If compilation fails:
1. Read the full error output
2. Identify missing dependencies or syntax errors
3. Fix the minimal set of issues to achieve compilation
4. Document any fixes made for future reference

Do NOT make architectural changes at this stage - only fix compilation blockers.

Expected outcome: Clean compilation with zero errors. Warnings are acceptable but should be noted.
  </action>
  <verify>
`cargo build --release -p ui_shell` completes with exit code 0.
Binary exists at `target/release/ui_shell`.
  </verify>
  <done>
Application compiles successfully. Binary size and any warnings recorded in SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Launch application and verify window</name>
  <files>None (verification only)</files>
  <action>
Run the compiled binary: `./target/release/ui_shell`

Verify:
1. Application launches without crashing
2. Main window appears on screen
3. Window displays basic UI elements (editor pane, status bar visible)
4. Application responds to window close (Cmd+Q works)

If launch fails:
1. Check for runtime errors in stderr
2. Check if GPUI requires specific macOS version or permissions
3. Document any runtime requirements discovered

Use `RUST_LOG=debug ./target/release/ui_shell 2>&1 | head -100` to capture initial logs.
  </action>
  <verify>
Application launches, displays window, and can be closed cleanly.
No crash reports or panic messages.
  </verify>
  <done>
Application launches on macOS. Window dimensions, initial state, and any runtime observations documented.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document baseline behavior and metrics</name>
  <files>None (documentation only)</files>
  <action>
Record baseline metrics for the application:

1. Binary size: `ls -lh target/release/ui_shell`
2. Startup time: Rough observation of time to window display
3. Memory usage: Use Activity Monitor or `ps aux | grep ui_shell` during run
4. Any visual observations about initial UI state

This establishes baseline for measuring improvements in later phases.

Also note:
- macOS version tested
- Architecture (Apple Silicon or Intel)
- Any warnings during compilation
- Any runtime quirks observed
  </action>
  <verify>
Baseline metrics captured and ready for SUMMARY.
  </verify>
  <done>
Baseline established: binary size, approximate startup time, initial memory, platform details.
  </done>
</task>

</tasks>

<verification>
1. `cargo build --release -p ui_shell` succeeds
2. `./target/release/ui_shell` launches and displays window
3. Application can be closed cleanly with Cmd+Q
4. No panic messages in logs
</verification>

<success_criteria>
- Application compiles on macOS (Apple Silicon and/or Intel confirmed)
- Application launches and displays main window
- Baseline metrics documented for comparison in later phases
- Any compilation fixes are minimal and documented
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-validation/01-01-SUMMARY.md`
</output>
