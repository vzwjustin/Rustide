---
phase: 01-foundation-validation
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Application compiles and launches on macOS (Apple Silicon and Intel)"
    - "Opening a 100K+ line file does not freeze the UI"
    - "Scrolling through a 100K+ line file renders smoothly"
    - "EditorPane displays actual file content from Document objects"
  artifacts: []
  key_links: []
---

<objective>
Integration testing of complete Phase 1 functionality with user verification.

Purpose: Validate all four success criteria are met in an integrated scenario. This is the final gate before Phase 1 completion, ensuring async loading and virtual scrolling work together correctly.

Output: Verified working foundation with documented performance metrics, ready for Phase 2 (Core Text Editing).
</objective>

<execution_context>
@/Users/justinadams/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinadams/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-validation/01-RESEARCH.md
@.planning/phases/01-foundation-validation/01-03-SUMMARY.md
@.planning/phases/01-foundation-validation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive test files</name>
  <files>None (test artifacts only)</files>
  <action>
Create a suite of test files for integration testing:

1. Small file (quick verification):
   ```bash
   echo "Hello, World!
   This is a small test file.
   Three lines total." > /tmp/rustide_test_small.txt
   ```

2. Medium file (1K lines):
   ```bash
   seq 1 1000 | while read i; do echo "Line $i: Medium file content"; done > /tmp/rustide_test_medium.txt
   ```

3. Large file (100K lines - primary test):
   ```bash
   seq 1 100000 | while read i; do echo "Line $i: Large file test - validates async loading and virtual scrolling"; done > /tmp/rustide_test_large.txt
   ```

4. Very large file (500K lines - stress test):
   ```bash
   seq 1 500000 | while read i; do echo "Line $i: Stress test content"; done > /tmp/rustide_test_stress.txt
   ```

5. Real-world file (Rust source):
   ```bash
   # Use the actual editor_pane.rs as test file
   cp crates/ui_shell/src/editor_pane.rs /tmp/rustide_test_rust.rs
   ```

Verify files created:
   ```bash
   wc -l /tmp/rustide_test_*.txt /tmp/rustide_test_rust.rs
   ```
  </action>
  <verify>
Test files exist with correct line counts.
  </verify>
  <done>
Test file suite created: small, medium, large, stress, and real Rust source.
  </done>
</task>

<task type="auto">
  <name>Task 2: Automated integration test sequence</name>
  <files>None (testing only)</files>
  <action>
Run automated tests to verify all success criteria:

1. Build release binary:
   ```bash
   cargo build --release -p ui_shell
   ```

2. Record baseline metrics:
   - Binary size: `ls -lh target/release/ui_shell`
   - Start the app and note startup time

3. Test file opening sequence (manual observation during run):
   - Open small file - content should appear instantly
   - Open medium file - content should appear quickly
   - Open large file (100K) - loading indicator should show, UI should stay responsive
   - Open stress file (500K) - same behavior, longer load time

4. For each file, verify:
   - Correct line count displayed
   - Content matches file (spot check lines 1, 100, 1000, etc.)
   - Scroll to end works
   - Scroll remains smooth

5. Performance logging (add temporary logging if needed):
   - Time from click to loading indicator
   - Time from click to content display
   - Frame rate during rapid scrolling

Document all findings for the integration test report.
  </action>
  <verify>
All test files open correctly.
UI never freezes during any operation.
Scrolling is smooth for all file sizes.
  </verify>
  <done>
Automated tests passed. Performance metrics documented.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 1 foundation: async file loading, virtual scrolling, Document entity wiring</what-built>
  <how-to-verify>
1. Run the application:
   ```bash
   ./target/release/ui_shell
   ```

2. Test Criterion 1 - macOS launch:
   - Application should launch without errors
   - Main window should appear
   - Should work on your Mac (note if Apple Silicon or Intel)

3. Test Criterion 2 - Async loading (no freeze):
   - Open the large test file: /tmp/rustide_test_large.txt
   - While loading, try to resize the window
   - Window should be responsive (not frozen)
   - "Loading..." should appear briefly
   - Content should appear after load

4. Test Criterion 3 - Virtual scrolling (smooth scroll):
   - With the large file open, scroll rapidly up and down
   - Use trackpad/mouse wheel and scrollbar
   - Scrolling should be smooth, no stuttering
   - Scroll to line 50,000 (middle of file) - should be instant

5. Test Criterion 4 - Real content:
   - Verify line 1 shows "Line 1: Large file test..."
   - Verify line 100000 shows "Line 100000: Large file test..."
   - Line numbers should match content

6. Bonus - Open the stress test file (500K lines):
   - Should handle even larger files
   - Note any performance differences

Report any issues found. All four criteria must pass for Phase 1 completion.
  </how-to-verify>
  <resume-signal>Type "approved" if all four criteria pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Application compiles and launches on macOS
2. Opening 100K+ line file shows loading indicator, UI stays responsive
3. Scrolling is smooth at all positions in large files
4. File content is correct (from Document, not placeholder)
5. All four success criteria verified by user
</verification>

<success_criteria>
All Phase 1 success criteria verified:
- [x] Application compiles and launches on macOS (Apple Silicon and Intel)
- [x] Opening a 100K+ line file does not freeze the UI (async loading works)
- [x] Scrolling through a 100K+ line file renders smoothly (virtual scrolling works)
- [x] EditorPane displays actual file content from Document objects (not placeholder strings)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-validation/01-05-SUMMARY.md`
</output>
