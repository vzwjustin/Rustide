---
phase: 02-core-text-editing
plan: 01
type: execute
wave: 1
depends_on: ["01-05"]
files_modified:
  - crates/ui_shell/src/editor_pane.rs
  - crates/ui_shell/src/theme.rs
autonomous: true

must_haves:
  truths:
    - "User can type text at cursor position and see it appear in the editor"
    - "Primary cursor is visible in the editor and blinks while focused"
    - "Backspace deletes the character before the cursor"
  artifacts:
    - path: "crates/ui_shell/src/editor_pane.rs"
      provides: "Keyboard/text input handling and cursor rendering"
      contains: "cursor_visible"
      contains: "insert"
      contains: "delete_backward"
    - path: "crates/ui_shell/src/theme.rs"
      provides: "Cursor color token"
      contains: "cursor"
  key_links:
    - from: "EditorPane input handler"
      to: "Document::insert"
      via: "document.update"
      pattern: "document\\.update\\(.*insert"
    - from: "EditorPane key handler"
      to: "Document::delete_backward"
      via: "backspace handling"
      pattern: "delete_backward"
    - from: "render_line"
      to: "cursor position"
      via: "caret element in active line"
      pattern: "cursor_visible"
---

<objective>
Wire keyboard text input to editor_core Document edits and render a blinking cursor.

Purpose: Phase 2 starts with a working typing loop. The editor must accept text input,
apply edits to Document, and display a visible caret that blinks while focused. This
establishes the user-visible editing feedback required by EDIT-01.

Output: EditorPane handles text input/backspace, Document is updated on each edit,
and render_line displays a blinking caret at the primary cursor position.
</objective>

<execution_context>
@/Users/justinadams/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinadams/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-text-editing/02-RESEARCH.md
@crates/ui_shell/src/editor_pane.rs
@crates/ui_shell/src/theme.rs
@crates/editor_core/src/document.rs
@crates/editor_core/src/cursor.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align cursor source of truth and status bar</name>
  <files>crates/ui_shell/src/editor_pane.rs</files>
  <action>
Use Document's cursor set as the single source of truth for cursor position:

1. Remove or stop using EditorTab.cursor (currently (line, col) tuple).
2. Add a helper that returns the active Document cursor position:
   - Use tab.document.read(cx).cursors().primary().position()
   - Keep values 0-indexed internally.
3. Update render_status_bar() to display line/column from Document cursor:
   - Convert to 1-indexed values for UI ("Ln 1, Col 1")
   - Use fallback (1, 1) when no document is open.
4. Ensure cx.notify() is called when cursor position changes.
  </action>
  <verify>
`cargo check -p ui_shell` passes.
Status bar reflects Document cursor values after edits.
  </verify>
  <done>
Status bar uses Document cursor state (no duplicate cursor source).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire text input and delete keys to Document edits</name>
  <files>crates/ui_shell/src/editor_pane.rs</files>
  <action>
Hook GPUI input events to editor_core Document operations:

1. Identify GPUI input event APIs for:
   - text input (character insertion)
   - key down for Backspace/Delete/Enter
2. Add EditorPane handlers that:
   - Call Document::insert(text) for text input
   - Call Document::delete_backward() for Backspace
   - Call Document::delete_forward() for Delete
   - Insert "\n" on Enter
3. Update EditorTab.is_modified after edits using Document::is_dirty().
4. Reset cursor blink to visible on any input to avoid "invisible caret".
5. Guard input when no tab is active or when tab.is_loading is true.
6. Call cx.notify() after each edit.
  </action>
  <verify>
Typing inserts text into the active document.
Backspace/Delete remove text without panics.
Editor remains responsive while editing.
  </verify>
  <done>
Keyboard input flows into Document edits with correct dirty state updates.
  </done>
</task>

<task type="auto">
  <name>Task 3: Render blinking cursor in editor lines</name>
  <files>crates/ui_shell/src/editor_pane.rs, crates/ui_shell/src/theme.rs</files>
  <action>
Implement caret rendering and blink state:

1. Add cursor_visible state to EditorPane (default true).
2. Add a cursor color token to Theme (reuse accent.primary or add ui.cursor).
3. Start a blink timer using GPUI's timer or async facilities:
   - Toggle cursor_visible every ~500ms while the pane is focused.
   - Pause blinking when EditorPane loses focus (cursor hidden or steady).
4. Update render_line() to render a caret for the active line:
   - Trim trailing newline from line_text.
   - Split the line at cursor column into prefix and suffix.
   - Insert a caret element between prefix and suffix when cursor_visible.
   - Handle empty lines and cursor-at-end-of-line.
5. Keep LINE_HEIGHT uniform to preserve virtual scrolling.
  </action>
  <verify>
Caret is visible in the active line and blinks while focused.
Caret remains correctly positioned after typing.
  </verify>
  <done>
Blinking caret rendered at the Document primary cursor position.
  </done>
</task>

<task type="auto">
  <name>Task 4: Manual verification of typing loop</name>
  <files>None (verification only)</files>
  <action>
Manual run to validate visible behavior:

1. Build and run the app:
   ```bash
   cargo build -p ui_shell
   ./target/debug/ui_shell
   ```
2. Open a text file in the editor pane.
3. Click inside the editor to focus.
4. Type a few characters and verify they appear.
5. Press Backspace and Delete to confirm deletion.
6. Press Enter to insert a newline.
7. Observe that the caret blinks while focused.
  </action>
  <verify>
Typing inserts text, Backspace/Delete remove text, caret blinks.
  </verify>
  <done>
Text input and cursor rendering verified manually.
  </done>
</task>

</tasks>

<verification>
1. EditorPane handles text input and Backspace/Delete via Document edits.
2. Status bar line/column comes from Document cursor state.
3. Blinking caret renders at the primary cursor position.
4. Manual typing verification passes.
</verification>

<success_criteria>
- User can type text at cursor position and see it appear in the editor.
- Primary cursor is visible and blinks while focused.
- Backspace deletes the character before the cursor without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-text-editing/02-01-SUMMARY.md`
</output>
